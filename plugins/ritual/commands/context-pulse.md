---
description: Score readiness / context debt for a feature ask before building.
argument-hint: "<feature ask>"
---

Load the ritual plugin's `ritual-build` skill (the Ritual dispatcher) and execute its `context-pulse` subcommand with these arguments: $ARGUMENTS
